export type Bank = {
    code: string;
    name: string;
}

export type Category = string;

export type City = {
    id: string;
    cityName: string;
}

export type Province = {
    id: string;
    provinceName: string;
}